{#
/**
 * @file
 * Default theme implementation of a dialog used to wrap child items.
 *
 * Available variables:
 * - attributes: HTML attributes for the containing element.
 * - children: The rendered child elements of the dialog.
 *
 * @see template_preprocess_exo_toolbar_dialog()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('exo-toolbar-item-dialog') }}>
  {{ children }}
</div>
