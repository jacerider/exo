<?php

namespace Drupal\{{ machine_name }}\ExoTheme\{{ plugin_id_camel_upper }};

use Drupal\exo\ExoTheme\ExoThemeBase;

/**
 * Plugin implementation of the '{{ plugin_id }}' eXo theme.
 *
 * @ExoTheme(
 *   id = "{{ plugin_id }}",
 *   label = @Translation("{{ label }}"),
 *   colors = {
{% for id, color in colors %}
 *     "{{ id }}" = "{{ color }}",
{% endfor %}
 *   }
 * )
 */
class {{ class }} extends ExoThemeBase {

}
